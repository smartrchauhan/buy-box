# Copy to .env.local and adjust if needed. .env.local is gitignored — never commit real secrets.
BUYBOX_ENV=local

DATABASE_URL=postgresql://buybox:buybox_local_only@localhost:5432/buybox

AWS_ENDPOINT_URL=http://localhost:4566
AWS_REGION=us-east-1
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

OFFERS_QUEUE_NAME=buybox-offer-updates-local
